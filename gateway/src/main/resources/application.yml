spring:
  cloud:
    gateway:
      routes:
      - id: user-service
        uri: http://localhost:8100
        predicates:
          - Path=/users/**
      - id: job-service
        uri: http://localhost:8000
        predicates:
          - Path=/jobs/**
      - id: application-service
        uri: http://localhost:8200
        predicates:
          - Path=/applications/**
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
    gateway:
      enabled: true